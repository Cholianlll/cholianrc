# Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.

syntax enable
set background=dark
" Plugins will be downloaded under the specified directory.
call plug#begin()

" Declare the list of plugins.

Plug 'preservim/nerdtree'
" Start NERDTree and put the cursor back in the other window.
" autocmd VimEnter * NERDTree | wincmd p

Plug 'vim-scripts/AutoComplPop'

Plug 'https://github.com/altercation/vim-colors-solarized.git'

Plug 'https://github.com/scrooloose/syntastic.git'

Plug 'vim-airline/vim-airline'

Plug 'tpope/vim-fugitive'

" List ends here. Plugins become visible to Vim after this call.
 NORMAL  ~/.vimrc                              vim  utf-8[unix]  3% ㏑:1/26☰℅:1
"~/.vimrc" 26L, 588B

" List ends here. Plugins become visible to Vim after this call.
call plug#end()

colorscheme solarized

~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
~
 COMMAND  ~/.vimrc[+]                       vim  utf-8[unix]  88% ㏑:24/27☰℅:15
Extension       Extern  Status
ale             0       not loaded
battery         0       not loaded
bookmark        0       not loaded
branch          0       loaded
bufferline      0       not loaded
capslock        0       not loaded
coc             0       not loaded
commandt        0       not loaded
csv             0       not loaded
ctrlp           0       not loaded
ctrlspace       0       not loaded
cursormode      0       not loaded
default         0       not loaded
denite          0       not loaded
dirvish         0       not loaded
eclim           0       not loaded
example         0       not loaded
fern            0       not loaded
fugitiveline    0       loaded
fzf             0       not loaded
gen_tags        0       not loaded
gina            0       not loaded
grepper         0       not loaded
gutentags       0       not loaded
hunks           0       not loaded
keymap          0       not loaded
languageclient  0       not loaded
localsearch     0       not loaded
lsp             0       not loaded
neomake         0       not loaded
netrw           0       loaded
nrrwrgn         0       not loaded
nvimlsp         0       not loaded
obsession       0       not loaded
omnisharp       0       not loaded
po              0       loaded
poetv           0       not loaded
promptline      0       not loaded
quickfix        0       loaded

syntax enable
set background=dark
" Plugins will be downloaded under the specified directory.
call plug#begin()

" Declare the list of plugins.

Plug 'preservim/nerdtree'
" Start NERDTree and put the cursor back in the other window.
" autocmd VimEnter * NERDTree | wincmd p

Plug 'vim-scripts/AutoComplPop'

Plug 'https://github.com/altercation/vim-colors-solarized.git'

Plug 'https://github.com/scrooloose/syntastic.git'

Plug 'vim-airline/vim-airline'

Plug 'tpope/vim-fugitive'

" List ends here. Plugins become visible to Vim after this call.
call plug#end()

colorscheme solarized

 NORMAL  ~/.vimrc[+]                        vim  utf-8[unix]  100% ㏑:27/27☰℅:1
" ==================== Editor behavior ====================
"ctrl+z撤销
map <C-Z> :u<CR>
map! <C-Z> <C-O>:u<CR>

"双击时高亮
map <2-leftmouse> \m
inoremap <2-leftmouse> <Esc>\m


" ==================== Terminal Plugins ====================
syntax enable
set background=dark
" Plugins will be downloaded under the specified directory.
call plug#begin()

" Declare the list of plugins.

Plug 'preservim/nerdtree'
" Start NERDTree and put the cursor back in the other window.
" autocmd VimEnter * NERDTree | wincmd p

Plug 'vim-scripts/AutoComplPop'
 NORMAL  ~/.vimrc[+]                          vim  utf-8[unix]  18% ㏑:7/37☰℅:1
" ==================== Editor behavior ====================
"ctrl+z撤销
map <C-Z> :u<CR>
map! <C-Z> <C-O>:u<CR>

"双击时高亮
map <2-leftmouse> \m
inoremap <2-leftmouse> <Esc>\m

"基本设置
set encoding=utf-8
set fileencoding=utf-8

" ==================== Terminal Plugins ====================
syntax enable
set background=dark
" Plugins will be downloaded under the specified directory.
call plug#begin()

" Declare the list of plugins.

Plug 'preservim/nerdtree'
" Start NERDTree and put the cursor back in the other window.
 COMMAND  ~/.vimrc[+]                        vim  utf-8[unix]  32% ㏑:13/40☰℅:1
:source %
" ==================== Editor behavior ====================
Last login: Wed Jan  4 11:59:09 on ttys000
 ~  cd Desktop                                                    ✔  base 

 ~/Desktop  ls                                                    ✔  base 
" ==================== Editor behavior ====================
"ctrl+z撤销
map <C-Z> :u<CR>
map! <C-Z> <C-O>:u<CR>

"双击时高亮
map <2-leftmouse> \m
inoremap <2-leftmouse> <Esc>\m

"基本设置
set encoding=utf-8
set fileencoding=utf-8

" ==================== Terminal Plugins ====================
syntax enable
set background=dark
" Plugins will be downloaded under the specified directory.
" ==================== Editor behavior ====================
"ctrl+z撤销
map <C-Z> :u<CR>
map! <C-Z> <C-O>:u<CR>

"双击时高亮
map <2-leftmouse> \m
inoremap <2-leftmouse> <Esc>\m

"基本设置
set encoding=utf-8
set fileencoding=utf-8

" ==================== Editor behavior ====================
"ctrl+z撤销
map <C-Z> :u<CR>
map! <C-Z> <C-O>:u<CR>

"双击时高亮
map <2-leftmouse> \m
inoremap <2-leftmouse> <Esc>\m

"基本设置
set encoding=utf-8
set fileencoding=utf-8

" ==================== Terminal Plugins ====================
syntax enable
set background=dark
" Plugins will be downloaded under the specified directory.
" ==================== Editor behavior ====================
"ctrl+z撤销
map <C-Z> :u<CR>
map! <C-Z> <C-O>:u<CR>

"双击时高亮
map <2-leftmouse> \m
inoremap <2-leftmouse> <Esc>\m

"基本设置
set encoding=utf-8
set fileencoding=utf-8

" ==================== Terminal Plugins ====================
syntax enable
set background=dark
" Plugins will be downloaded under the specified directory.
call plug#begin()

" Declare the list of plugins.

Plug 'preservim/nerdtree'
" Start NERDTree and put the cursor back in the other window.
" autocmd VimEnter * NERDTree | wincmd p

Plug 'vim-scripts/AutoComplPop'

Plug 'https://github.com/altercation/vim-colors-solarized.git'

Plug 'https://github.com/scrooloose/syntastic.git'

Plug 'vim-airline/vim-airline'

Plug 'tpope/vim-fugitive'

" List ends here. Plugins become visible to Vim after this call.
call plug#end()

colorscheme solarized
